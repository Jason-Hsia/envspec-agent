# 核心（必需）
# 无第三方依赖也可运行，以下为推荐增强项
jieba>=0.42.1          # 中文分词，缺失时降级为字符 bigram
rank_bm25>=0.2.2       # BM25 实现，缺失时使用内置简化版
numpy>=1.24            # 向量运算

# 可选：接入真实模型时启用
# sentence-transformers>=2.2
# openai>=1.30
# faiss-cpu>=1.7

# 可选：报告与可视化
# jinja2>=3.1
